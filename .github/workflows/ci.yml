name: CI - test, build and publish

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; fi

      - name: Run tests (if any)
        run: |
          if python -c "import pytest" >/dev/null 2>&1; then pytest -q || (echo 'Tests failed' && exit 1); else
            if [ -d tests ] || ls | grep -q 'test_' ; then
              python -m pip install pytest
              pytest -q || (echo 'Tests failed' && exit 1)
            else
              echo "No tests found, skipping pytest"
            fi
          fi

  build-and-publish-backend:
    needs: test
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT || github.token }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU (multi-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend image to GHCR and Docker Hub
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          platforms: linux/amd64
          no-cache: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/receipt-manager-backend:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/receipt-manager-backend:latest
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/receipt-manager-backend:${{ github.sha }}
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/receipt-manager-backend:latest

  build-and-publish-frontend:
    needs: test
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT || github.token }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU (multi-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push frontend image to GHCR and Docker Hub
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          platforms: linux/amd64
          no-cache: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/receipt-manager-frontend:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/receipt-manager-frontend:latest
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/receipt-manager-frontend:${{ github.sha }}
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/receipt-manager-frontend:latest